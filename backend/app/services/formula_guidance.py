FORMULA_OUTPUT_INSTRUCTIONS = """
公式输出规范：
1. 只要涉及数学、统计、物理、算法复杂度、概率、损失函数、矩阵、优化目标或评价指标，请使用 TeX 公式。
2. 行内公式使用 \\(...\\)，块级公式使用 \\[...\\]；不要用图片公式，不要只用 Unicode 上下标替代完整公式。
3. 如果当前接口要求输出 JSON，JSON 字符串里的反斜杠必须合法转义，例如写成 "\\\\(a^2+b^2=c^2\\\\)" 或 "\\\\[L=\\\\sum_i y_i\\\\log p_i\\\\]"。
4. 公式前后要用自然中文解释变量含义，避免只给公式不解释。
5. 不要使用 Markdown 代码块包裹公式。
"""

